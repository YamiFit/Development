import * as React from 'react'
import { cn } from '@/lib/utils'

// Minimal form stub - not used in this project
export const Form = ({ children }) => children
export const FormField = ({ children }) => children
export const FormItem = ({ children }) => children
export const FormLabel = ({ children }) => children
export const FormControl = ({ children }) => children
export const FormDescription = ({ children }) => children
export const FormMessage = ({ children }) => children